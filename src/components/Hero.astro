<section id="inicio" class="hero-section">
  <div class="container hero-container">
    <div class="hero-content">
      <h1 class="hero-title">Transforme sua presença digital com landing pages profissionais</h1>
      <p class="hero-description">
        Designs modernos, responsivos e otimizados para conversão que farão seu negócio se destacar da concorrência. 
        Entregamos resultados que convertem visitantes em clientes.
      </p>
      <div class="hero-cta">
        <a href="#projetos" class="btn btn-primary">Ver Projetos</a>
        <a href="#servicos" class="btn btn-secondary">Conhecer Serviços</a>
      </div>
    </div>
    
    <div class="hero-image">
      <img 
        src="https://images.unsplash.com/photo-1544256718-3bcf237f3974?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=2071&q=80" 
        alt="Ilustração de design web profissional"
        width="600"
        height="500"
      />
    </div>
  </div>
  
  <div class="scroll-indicator">
    <div class="mouse">
      <div class="wheel"></div>
    </div>
    <div class="arrow-scroll">
      <span></span>
      <span></span>
      <span></span>
    </div>
  </div>
</section>

<style>
  .hero-section {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    position: relative;
    overflow: hidden;
    padding: 120px 0 80px;
    background: linear-gradient(135deg, rgba(45, 52, 54, 0.02) 0%, rgba(113, 89, 193, 0.08) 100%);
  }
  
  .hero-container {
    display: grid;
    grid-template-columns: 6fr 4fr;
    gap: 4rem;
    align-items: center;
  }
  
  .hero-title {
    font-size: 4rem;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    background: linear-gradient(135deg, var(--cor-primaria), var(--cor-secundaria));
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    animation: fadeInUp 0.8s ease-out forwards;
  }
  
  .hero-description {
    font-size: 1.125rem;
    line-height: 1.6;
    margin-bottom: 2.5rem;
    max-width: 600px;
    animation: fadeInUp 0.8s ease-out 0.2s forwards;
    opacity: 0;
  }
  
  .hero-cta {
    display: flex;
    gap: 1rem;
    animation: fadeInUp 0.8s ease-out 0.4s forwards;
    opacity: 0;
  }
  
  .btn-secondary {
    background-color: transparent;
    border: 2px solid var(--cor-secundaria);
    color: var(--cor-primaria);
  }
  
  .btn-secondary:hover {
    background-color: var(--cor-secundaria);
    color: white;
    transform: translateY(-3px);
  }
  
  .hero-image {
    position: relative;
    animation: fadeInRight 1s ease-out 0.6s forwards;
    opacity: 0;
  }
  
  .hero-image img {
    width: 100%;
    height: auto;
    border-radius: 16px;
    box-shadow: var(--sombra-suave);
    transform: perspective(1000px) rotateY(-5deg);
    transition: var(--transicao-padrao);
  }
  
  .hero-image::before {
    content: '';
    position: absolute;
    top: -20px;
    right: -20px;
    width: 100%;
    height: 100%;
    border: 2px solid var(--cor-secundaria);
    border-radius: 16px;
    z-index: -1;
  }
  
  .scroll-indicator {
    position: absolute;
    bottom: 40px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    animation: fadeInUp 0.8s ease-out 1s forwards;
    opacity: 0;
  }
  
  .mouse {
    width: 26px;
    height: 40px;
    border: 2px solid var(--cor-primaria);
    border-radius: 20px;
    display: flex;
    justify-content: center;
    padding-top: 10px;
  }
  
  .wheel {
    width: 4px;
    height: 8px;
    background-color: var(--cor-secundaria);
    border-radius: 2px;
    animation: scrollWheel 1.5s infinite;
  }
  
  .arrow-scroll {
    margin-top: 5px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 3px;
  }
  
  .arrow-scroll span {
    display: block;
    width: 10px;
    height: 10px;
    border-bottom: 2px solid var(--cor-primaria);
    border-right: 2px solid var(--cor-primaria);
    transform: rotate(45deg);
    animation: fadeIn 0.6s alternate infinite;
    opacity: 0;
  }
  
  .arrow-scroll span:nth-child(2) {
    animation-delay: 0.2s;
  }
  
  .arrow-scroll span:nth-child(3) {
    animation-delay: 0.4s;
  }
  
  @keyframes scrollWheel {
    0% {
      opacity: 1;
      transform: translateY(0);
    }
    100% {
      opacity: 0;
      transform: translateY(10px);
    }
  }
  
  @keyframes fadeIn {
    0% {
      opacity: 0;
    }
    100% {
      opacity: 1;
    }
  }
  
  @keyframes fadeInUp {
    0% {
      opacity: 0;
      transform: translateY(30px);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  @keyframes fadeInRight {
    0% {
      opacity: 0;
      transform: translateX(50px);
    }
    100% {
      opacity: 1;
      transform: translateX(0);
    }
  }
  
  @media (max-width: 1024px) {
    .hero-container {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
    
    .hero-title {
      font-size: 3rem;
    }
    
    .hero-image {
      order: -1;
      max-width: 80%;
      margin: 0 auto;
    }
  }
  
  @media (max-width: 768px) {
    .hero-section {
      padding: 100px 0 60px;
    }
    
    .hero-title {
      font-size: 2.5rem;
    }
    
    .hero-image {
      max-width: 100%;
    }
  }
  
  @media (max-width: 480px) {
    .hero-title {
      font-size: 2rem;
    }
    
    .hero-cta {
      flex-direction: column;
    }
    
    .hero-image::before {
      display: none;
    }
  }
</style> 